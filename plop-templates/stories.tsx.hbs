import type { StoryObj, Meta } from '@storybook/react'
import { within } from '@storybook/testing-library'
import { expect } from '@storybook/jest'
import {{pascalCase name}}Component from './{{pascalCase name}}'

const meta: Meta<typeof {{pascalCase name}}Component> =  {
  title: 'Components/{{pascalCase name}}',
  component: {{pascalCase name}}Component
} 

export default meta

type Story = StoryObj<typeof {{pascalCase name}}Component>

export const {{pascalCase name}}: Story = {
    play: async ({canvasElement, step}) => {
        const canvas = within(canvasElement)
        const title = canvas.getByRole('heading', { name: '{{pascalCase name}}' })

        await step('Render heading', () => {
            expect(title).toBeInTheDocument()
        })
    }
}